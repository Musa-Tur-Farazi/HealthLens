# Lightweight requirements for 512MB deployments (Render free tier)
# Only core vision models, no LLM/CLIP

fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# PyTorch CPU-only (smaller)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Vision models
timm>=1.0.0

# Core utilities
Pillow>=10.0.0
python-multipart>=0.0.9
requests>=2.31.0
gdown>=5.0.0

# Optional: Only if ENABLE_LLM=1 (NOT recommended for 512MB)
# transformers>=4.42.0
# sentencepiece>=0.2.0

# Optional: Only if ENABLE_GRADCAM=1
# grad-cam>=1.4.0

